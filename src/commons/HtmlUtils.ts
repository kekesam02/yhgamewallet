

/**
 * html 的一些公共方法
 */
class HtmlUtils {

    /**
     * 字符串填充到指定位数
     * @param str: 字符串
     * @param num: 字符串填充到当前位数
     * @param type:
     */
    public stringFill = (str: string, num: number) => {
        // if (str.length < num) {
        //     str.padEnd()
        // }
    }
}


export default HtmlUtils
